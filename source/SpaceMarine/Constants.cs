namespace DeenGames.SpaceMarine
{
    public static class Constants
    {
        // These are the base values for starting difficulty (100?)
        internal const int MAP_TILES_WIDE = 60;
        internal const int MAP_TILES_HIGH = 50;

        // (960x528) with (32, 32) tiles
        internal const int DISPLAY_TILES_WIDE = 30;
        internal const int DISPLAY_TILES_HIGH = 16;

        internal const int STATUS_BAR_HEIGHT = 48;

        internal const int TILE_WIDTH = 8;
        internal const int TILE_HEIGHT = 16;

        internal const int GAME_ZOOM = 1;
    }
}